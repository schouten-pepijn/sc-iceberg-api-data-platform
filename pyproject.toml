[project]
name = "sc-iceberg-api-data-platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "great-expectations>=1.15.1",
    "httpx>=0.28.1",
    "pandas>=3.0.1",
    "pyiceberg>=0.11.1",
]

[dependency-groups]
dev = [
    "bump-my-version>=1.2.7",
]
